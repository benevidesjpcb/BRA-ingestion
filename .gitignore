.Rhistory
.RData
.Ruserdata

.Rproj.user/

# Secrets — holds TATIC_TOKEN; must never be committed
.Renviron

data-raw/*
!data-raw/.gitkeep
# Track the analytic CSV outputs in data/, but not the large parquet extracts.
data/apdf/
outputs/*
!outputs/.gitkeep
temp/

# KPI08 (ODIN) downloaded data — keep the script, not the raw pulls
API_kpi08_ICEA/*.json
API_kpi08_ICEA/*.csv